import { EPSILON } from "../SymbolValidator";
import Grammar from "../Grammar"

// PROBLEM ===> DAR ESPAÇO NO LADO DIREITO DE ENTRADA DE TEXTO (SEM QUEBRAR LINHA)

export function removeUseless(grammar) {
    let newGrammar = new Grammar();
    let prodAux = [];

    return newGrammar;
}